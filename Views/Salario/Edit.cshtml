@model ProjetoDespesas.Models.Salario

@{
    ViewData["Title"] = "Editar salario";
}

<div class="formulario">
<h4>Salario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form class="form rounded border" asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SalarioId" />
            <div class="form-group">
                <label asp-for="MesId" class="control-label">Mês</label>
                <select asp-for="MesId" class="form-control" asp-items="ViewBag.MesId"></select>
                <span asp-validation-for="MesId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Valor" class="control-label"></label>
                <input asp-for="Valor" class="form-control" />
                <span asp-validation-for="Valor" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-success" /> &nbsp;
                <a class="btn btn-outline-dark btn-sm" asp-action="Index">Back to List</a>

            </div>
        </form>
    </div>
</div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
